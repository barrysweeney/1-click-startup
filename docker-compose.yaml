version: "3.7"
services:
  frontend:
    build: ./1cs-frontend
    volumes:
      - ./1cs-frontend/src:/app/src
    ports:
      - "8000:8000"
  backend:
    build:
      context: ./1cs-backend
      dockerfile: Dockerfile.dev
    volumes:
      - ./1cs-backend/src:/app/src
    ports:
      - "5000:5000"
  landing:
    build:
      context: ./landing
      dockerfile: Dockerfile.dev
    volumes:
      - ./landing/src:/app/src
    ports:
      - "4200:4200"
  
